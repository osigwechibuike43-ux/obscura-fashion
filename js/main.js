// A simple function to simulate adding an item to the cart
function addToCart() {
    const size = document.getElementById('size').value;
    alert(`Item added to cart! Size selected: ${size}`);
    
    // In a real application, you would save this data to localStorage 
    // or send it to a backend database here.
}
